# 更新日志

本文档记录斑马资源库的重要功能变化、体验优化和问题修复。

本文档按照公开版本记录功能变化；发布前的功能仍可能根据验收结果调整。

## 0.1.0（2026-08-25）

### 新增功能

- 支持斑马百科、斑马口语和斑马 AI 学三个产品，并为每个产品保存独立登录状态。
- 支持浏览课程包、专辑、课程、课节和学习任务。
- 支持点击面包屑中的中间目录返回对应课程层级。
- 支持选择保存目录和设置并发下载数量。
- 支持按 `4K`、`1080P`、`720P`、标清多选视频清晰度。
- 支持选择中文、英文或全部视频语言。
- 支持批量选择专辑，并按照专辑顺序逐个完成下载。
- 支持“仅解析所选专辑”，可以先检查资源再决定是否下载。
- 支持显示专辑解析状态、课程进度、下载状态、已写入大小和失败原因。
- 支持停止全部任务、取消单个任务以及重试失败或中断任务。
- 支持预览在线图片、在线音频和已经下载完成的视频。
- 支持从应用中直接打开下载目录。

### 下载体验优化

- 下载内容按照“专辑 / 子课程”自动建立目录，避免不同课程资源混在一起。
- 文件名增加课程顺序编号，确保连续课程能够按正确顺序播放。
- 视频文件名增加中文、英文和清晰度标识。
- 同一课程视频的多个清晰度在界面中合并展示，并按一个视频计算数量。
- 中文和英文版本在界面中合并为同一课程视频，同时保留独立下载选择。
- 多专辑下载改为专辑之间依次执行，当前专辑内部保持并发，并优先下载视频。
- 支持临时文件续传，网络中断后重试时可减少重复传输。
- 下载成功前会检查文件完整性和视频可播放性，损坏文件不会被标记为完成。

### 资源列表优化

- 优化课程视频标题，减少无意义的随机文件名。
- 过滤视频时间轴截图、界面装饰图片和会员引导图片。
- 过滤课程开场、章节转场和界面动画素材。
- 过滤三维互动模型、动画运行包及其他不能独立使用的数据文件。
- 过滤互动答题提示音、局部反馈音和奖励弹窗音频。
- 过滤推荐课程入口，避免其他课程内容混入当前专辑。
- 过滤同一视频的重复地址，并为每个清晰度保留一个有效来源。
- 隐藏流媒体清单格式，下载结果统一保存为用户可播放的媒体文件。

### 问题修复

- 修复部分视频下载完成后无法播放、画面损坏或播放器提示文件格式错误的问题。
- 修复网络连接提前中断时仍将不完整视频标记为成功的问题。
- 修复部分流媒体下载参数不兼容导致全部视频失败的问题。
- 修复选择专辑后部分子课程没有被解析和下载的问题。
- 修复专辑与可下载资源状态不同步的问题。
- 修复“清空全部”只清除专辑、没有同步清除资源的问题。
- 修复多个清晰度被误认为多个独立视频的问题。
- 修复选择指定清晰度和语言时仍下载同组无标签兜底视频、导致中英文 4K 各下载两份的问题。
- 修复中文和英文视频名称相同、无法区分的问题。
- 修复窗口缩小时主要内容和下载区域显示不完整的问题。
- 修复批量选择大量专辑后启动等待时间过长的问题。
- 修复点击停止后排队任务、网络流或媒体处理进程仍继续运行的问题。
- 修复直接执行 `cargo tauri build` 时 MSI 使用 `en-US` 导致中文产品名称打包失败的问题。
- 修复 Release 主程序以及 FFmpeg、Bento4 子进程运行时弹出 CMD 窗口的问题。

### 界面优化

- 调整主导航、课程内容区和下载队列的布局，提高信息利用率。
- 支持窗口宽度变化，在较小窗口下保持主要功能可用。
- 增加专辑下载进度面板，集中展示每个专辑的完成情况。
- 统一课程资源的标题、类型、语言、清晰度和状态展示。
- 优化空状态、加载状态、失败状态和停止状态提示。

### 工程质量

- 将桌面端后台逻辑按认证、课程目录、资源解析、下载、媒体处理、文件系统和状态管理拆分。
- 将前端资源筛选逻辑、专辑进度、资源行和预览组件从主页面中拆分。
- 开发模式增加下载、媒体处理和失败原因日志，便于定位问题。
- 会话 Cookie 改用 Windows DPAPI 加密保存，并自动迁移旧版明文会话。
- 收紧页面 CSP 和本地资源访问范围，仅在预览时授权下载目录内的具体文件。
- Windows 安装包构建会自动携带 FFmpeg、mp4dump 和 mp4decrypt，用户无需安装外部依赖。
- 同时提供 NSIS、MSI 和便携式 ZIP 三种 Windows 分发格式。
- 增加 `pnpm clean`，统一清理可再生成的构建与测试产物。
- 增加课程资源过滤、视频变体、语言识别、文件命名、任务停止、前端组件和完整下载流程测试。
- 增加统一格式化命令，覆盖前端、配置、文档、Rust 与 PowerShell 脚本。
- 增加 GitHub Actions，自动执行全仓格式检查、前端构建与测试、Rust 静态检查和单元测试。
- 清理未使用的资源扫描入口和项目依赖。
- 补充面向用户的项目说明、使用步骤、常见问题和隐私说明。

### 当前说明

- `0.1.0` 是项目首次公开发布版本。
- Windows 是当前主要测试平台。
- 正式安装包会携带媒体处理依赖；开发环境说明请查看 [README](README.md)。
